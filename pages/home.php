<div class="hero-unit">
	<h1>Overview</h1>
	<p>A quick PHP reference guide to gain a basic grasp of foundational concepts.  After poking around a bit, 
		you might feel compelled to add a bit of your own code snippets. Use the following as a step-by-step 
		guide to do so:</p>
	<ul>
		<li>Add a PHP file to the <code>/code/snippets/</code> directory, for example <strong>regex.php</strong></li>
		<li>Type your PHP code into the <strong>.php</strong> file.</li>
		<li>In <code>/layout/nav.php</code>, add your page into the <code>$nav</code> array under the appropriate category, or 
			create a new one. See the existing nav items for examples.</li>
		<li><strong>TEST!</strong></li>
	</ul>
</div>